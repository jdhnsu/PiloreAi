import "dotenv/config";
import readline from "node:readline/promises";
import { createCodeMentorSession, getDefaultCodeProfiles, type SessionEvent } from "../../index.js";
const CYAN = "\x1b[36m", MAGENTA = "\x1b[35m", RESET = "\x1b[0m";
function render(event: SessionEvent): void { if (event.type === "text_delta") process.stdout.write(event.delta); else if (event.type === "message_end") process.stdout.write("\n"); else if (event.type === "tool_start") process.stdout.write(`\n[工具] ${event.toolName}\n`); else if (event.type === "tool_end") process.stdout.write(`${event.isError ? "[错误]" : "[结果]"} ${event.text}\n`); else if (event.type === "profile") process.stdout.write(`\n[导师] ${event.name ?? "自动路由"}\n`); else if (event.type === "toolset") process.stdout.write(`\n[工具组] ${event.toolset} 已加载\n`); else if (event.type === "error") process.stdout.write(`\n[错误] ${event.message}\n`); }
export function main(): void { const profiles = getDefaultCodeProfiles(); const session = createCodeMentorSession(); const help = `命令: /quit /abort /help\n导师:\n${profiles.map((p) => `  @${p.key} 问题  指定 ${p.name}`).join("\n")}\n  @pilore 问题  自动路由`; console.log(`PiLore Code Mentor 已就绪（${session.modelInfo}）\n${help}`); const rl = readline.createInterface({ input: process.stdin, output: process.stdout }); const ask = () => process.stdout.write(`${session.profile ? `${MAGENTA}[${session.profile}]${RESET} ` : ""}${CYAN}❯${RESET} `); ask(); rl.on("line", (line) => { const text = line.trim(); if (!text) return ask(); if (text === "/quit") { rl.close(); return; } if (text === "/help") { console.log(help); return ask(); } if (text === "/abort") { session.abort(); return; } if (session.busy) { console.log("正在处理上一条消息"); return; } void session.prompt(text, render).finally(ask); }); }
if (import.meta.url === `file://${process.argv[1]?.replace(/\\/g, "/")}`) main();
